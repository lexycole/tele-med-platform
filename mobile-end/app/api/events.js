// import api from "./client";

// const endpoint = "/events";

// export const getEvents = () => api.get(endpoint);

// export const getEvent = (id) => api.get(`${endpoint}/${id}`);

// export const postEvent = (data) => api.post(endpoint, data);

// export const updateEvent = (id, data) =>
//   api.put(`${endpoint}/${id}`, data);

// export const deleteEvent = (id) => api.delete(`${endpoint}/${id}`);
